{
  "name": "ai-hugging-face",
  "version": "1.0.0",
  "main": "dist/ollama.js",
  "scripts": {
    "build:client": "tsc -p tsconfig.client.json",
    "build:server": "tsc -p tsconfig.json",
    "build": "npm run build:client && npm run build:server",
    "watch:client": "tsc -p tsconfig.client.json --watch",
    "dev": "npm run build:client && ts-node server.ts",
    "start": "node dist/server.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@huggingface/inference": "^4.13.15",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
